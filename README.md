# Mototaxi Project - FastAPI (Supatá)

## Descripción
Proyecto base en FastAPI para gestión de mototaxis. Incluye modelos, CRUD, soft-delete y endpoint de reportes (CSV).

## Requisitos
- Python 3.10+
- pip install -r requirements.txt

## Ejecutar
1. `pip install -r requirements.txt`
2. Desde la carpeta `backend` ejecutar:
   `uvicorn main:app --reload --port 8000`
3. Abrir: `http://127.0.0.1:8000/docs` para ver documentación interactiva.
